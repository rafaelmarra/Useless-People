<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.example.rafaelmarra.useless_people">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/dilbert"
            android:label="@string/app_name"
            android:roundIcon="@drawable/dilbert"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.Barless"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.example.rafaelmarra.useless_people.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>